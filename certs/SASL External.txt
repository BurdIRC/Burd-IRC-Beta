In order to use SASL External you must have a certificate file and key file (.crt and .key) with a name that matches the name you gave the network.

For example if I name a network "freenode" I would need to name the files freenode.crt and freenode.key